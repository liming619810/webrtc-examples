┌─────────────┐           信令传输（手动复制粘贴/WebSocket）          ┌─────────────┐
│  浏览器     │                                                       │  Pion Go   │
└─────┬───────┘                                                       └──────┬──────┘
      │                                                                      │
      │  1. 浏览器创建 Offer SDP                                             │
      │  (包含: 音视频编码、ICE 候选、指纹等)                                 │
      │ ───────────────────────────────────────────────────────────────────► │
      │                                                                      │
      │                                                                 2. Pion 解析 Offer
      │                                                                     │  SetRemoteDescription(offer)
      │                                                                     │
      │                                                                 3. Pion 创建 Answer SDP
      │                                                                     │  包含: 支持的编码、ICE、指纹
      │                                                                     │
      │ ◄────────────────────────────────────────────────────────────────── │
      │  4. 浏览器接收 Answer                                                │
      │  SetRemoteDescription(answer)                                       │
      │                                                                      │
      │                                                                      │
┌─────▼───────┐                                                       ┌──────▼──────┐
│            【ICE 连通阶段】                                         │             │
└─────┬───────┘                                                       └──────┬──────┘
      │                                                                      │
 5. 浏览器收集 ICE Candidates                                     6. Pion 收集 ICE Candidates
 (本地IP、内网IP、STUN 外网IP)                                        (本地IP、内网IP、STUN 外网IP)
      │                                                                      │
      │  互相交换 Candidate（通过信令）                                      │
      │ ◄──────────────────────────────────────────────────────────────► │
      │                                                                      │
      │                     7. 双向连通性检查 (STUN Binding)                 │
      │ ◄───────────────────────── UDP 连通测试 ────────────────────────────► │
      │                                                                      │
      │                8. 选出最优通路 → ICE 状态变为 Connected               │
      │                                                                      │
      │                    ↓（你代码里 iceConnectedCtxCancel()）              │
      │                                                                      │
┌─────▼───────┐                                                       ┌──────▼──────┐
│            【DTLS 握手阶段】                                        │             │
└─────┬───────┘                                                       └──────┬──────┘
      │                                                                      │
 9. 自动发起 DTLS 握手                                             10. 响应 DTLS 握手
 (UDP 上的 TLS，带证书、指纹验证)
      │                                                                      │
      │ ◄───────────────────────── DTLS 握手包 ────────────────────────────► │
      │                                                                      │
      │                  11. DTLS 握手成功 → 安全通道建立                    │
      │                                                                      │
      │                    ↓（从 DTLS 中导出 SRTP 密钥）                     │
      │                                                                      │
┌─────▼───────┐                                                       ┌──────▼──────┐
│            【SRTP 音视频传输】                                       │             │
└─────┬───────┘                                                       └──────┬──────┘
      │                                                                      │
                                                              12. Pion 开始发流
                                                                   读取 output.h264/ogg
                                                                      │
                                                                      │
                                                                      ▼
                                                            13. 封装为 RTP 包
                                                                      │
                                                                      ▓
                                                            14. SRTP 加密（AES）
                                                                      │
                                                                      ▼
                                                            15. 通过 UDP 发送
                                                                      │
      │ ◄──────────────────────── SRTP 加密包 ───────────────────────── │
      │                                                                      │
 16. 浏览器接收 UDP 包                                                  │
      │
      ▼
 17. SRTP 解密 → 得到 RTP
      │
      ▼
 18. 解封装 RTP → 获取 H.264/Opus
      │
      ▼
 19. 解码 → 播放声音和画面
